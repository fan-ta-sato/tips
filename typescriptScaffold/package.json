{
  "name": "tstest",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "npm run lint && npm run compile",
    "fmtbuild": "npm run fmt && npm run compile",
    "compile": "tsc src/**.ts",
    "lint": "tslint --project tsconfig.json 'src/**.ts'",
    "fmt": "tslint --fix 'src/**.ts'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "prettier": "^1.13.7",
    "tslint": "^5.10.0",
    "tslint-config-airbnb": "^5.9.2",
    "tsutils": "^2.27.2",
    "typescript": "^2.9.2"
  },
  "devDependencies": {
    "tslint-config-prettier": "^1.13.0",
    "tslint-plugin-prettier": "^1.3.0"
  }
}
